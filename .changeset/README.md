# Changesets

This folder holds pending release notes.

Run `npm run changeset` from the repo root to add a changeset when a PR should bump the package version. See the **Release** section in the [README](../README.md).
